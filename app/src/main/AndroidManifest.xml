<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sportstimer">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_EasterEgg"></activity>
        <activity android:name=".TimerSetActivity" />
        <activity android:name=".QuoteActivity" />
        <activity android:name=".IntervalActivityHome" />
        <activity android:name=".MyFavoriteActivity" />
        <activity android:name=".IntervalActivity" />
        <activity android:name=".StopWatchActivity" />
        <activity android:name=".TimerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>